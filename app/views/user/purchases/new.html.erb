
<h2>購入確認</h2>

<div class="col-xs-2">
	<div><%= attachment_image_tag @item, :image, size: "200x200" %></div>
</div>

<div class="col-xs-6">

<table class="table table-bordered">
	<tr>
		<th>名前</th>
		<td><%= @item.item_name %></td>
	</tr>
	<tr>
		<th>値段</th>
		<td><%= @item.price %></td>
	</tr>
	<tr>
		<th>送料</th>
		<td>未定</td>
	</tr>
</table>

<p class="tyui">
	購入後、ご登録いただきましたメールアドレスに請求金額を送信致します。<br>
	※確認の上、商品の金額＋送料を請求させていただきます。ご了承ください。
</p>


<%= form_for(@purchase, url:user_purchases_path(@purchase)) do |f| %>
	<%= f.hidden_field :item_id, :value => @item.id %>
	<%= f.hidden_field :user_id, :value => @user.id %>

<%# form_for(@item, url:user_purchase_path(@item)) do |f| %>
    <%# f.hidden_field :selling_status, :value => false %>
<div class="kounyu">
	<%= f.submit "購入確定",class: "btn btn-info" %>
</div>

<%# end %>
<% end %>
</div>
