

<table>
	<tr>
		<th>商品画像</th>
		<th>名前</th>
	</tr>

	<% @items.each do |item| %>
	<tr>
		<td><%= attachment_image_tag item, :image, size: "100x100" %></td>
		<td><%= link_to "#{item.item_name}", user_item_path(item) %></td>
	</tr>
	<% end %>
</table>


