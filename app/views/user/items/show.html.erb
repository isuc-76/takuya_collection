

<table>
	<tr>
		<td><%= attachment_image_tag @item, :image, size: "200x200" %></td>
	</tr>
</table>

<table>
	<tr>
		<th>名前</th>
		<td><%= @item.item_name %></td>
	</tr>
	<tr>
		<th>説明</th>
		<td><%= @item.content %></td>
	</tr>
	<tr>
		<th>値段</th>
		<td><%= @item.price %></td>
	</tr>
</table>

<%= link_to "購入", new_user_purchase_path(@item.id) %>

<%# link_to "購入", user_purchases_path(user_id:current_user, item_id: @item) %>

