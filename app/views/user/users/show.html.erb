<%= render 'layouts/sidebar' %>

<div class="mypege">

	<table>
		<tr>
			<th>名前</th>
			<td><%= @user.name %></td>
		</tr>
		<tr>
			<th>電話番号</th>
			<td><%= @user.telephone_number %></td>
		</tr>
		<tr>
			<th>郵便番号</th>
			<td><%= @user.postal_code %></td>
		</tr>
		<tr>
			<th>住所</th>
			<td><%= @user.address %></td>
		</tr>
		<tr>
			<th>メールアドレス</th>
			<td><%= @user.email %></td>
		</tr>
	</table>

	<%= link_to"編集", edit_user_user_path(current_user) %>

	<%= link_to"お買い上げ商品一覧", user_purchases_path(current_user) %>


	<%# link_to"退会する", user_user_path(@user), method: :delete %>

</div>

